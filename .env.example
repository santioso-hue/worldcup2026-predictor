# Copia este archivo a `.env` y rellena tus claves. `.env` está en .gitignore.
# Las claves se usan server-side; NUNCA se commitean.

# football-data.org (v4) — proveedor LIVE (free, cubre el WC2026 sin límite de
# temporada). Header de auth: X-Auth-Token. Regístrate gratis en:
# https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=
