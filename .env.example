# Copy this file to `.env` and fill in your keys. `.env` is in .gitignore.
# Keys are used server-side; NEVER commit them.

# football-data.org (v4) — LIVE data provider (free, covers WC2026 with no
# season limit). Auth header: X-Auth-Token. Register for free at:
# https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=
